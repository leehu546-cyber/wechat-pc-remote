# Cursor Worker（已停用 §82）

2026-06-30 尝试通过桌面粘贴 + OCR 把微信任务交给 Cursor Composer。
因额度弹窗、坐标漂移、假提交、换号耗时长等问题不稳定，已回退。

**当前策略：** 机械任务 → 固定 `scripts/*.ps1`；其余 → Planner + OpenCode；重复需求再沉淀为新脚本。

保留本目录仅供查阅，WeClaw 不再调用。
